#include<bits/stdc++.h>
using namespace std;
int main()
{
    vector <int> v;
    int a,k; 
    cin>>k;
    while(1)
    {
        cin>>a;
        if(a==-1)
            break;
        v.push_back(a);
    }
    sort(v.begin(),v.end());
    cout<<v[k-1]<<endl;
    
return 0;
    
}
